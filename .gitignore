# Connect IQ build artifacts (the store deliverable lives in store/ and IS committed)
bin/
*.prg
*.prg.debug.xml
*.iq.debug.xml

# Developer signing keys must NEVER be committed
*.der
*.pem
developer_key.*

# Python bytecode (from scripts/*.py, e.g. the CI test-gate parser)
__pycache__/
*.pyc
